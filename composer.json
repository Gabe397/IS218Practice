{
    "name": "gabe3/is218practice",
    "require": {
        "phpunit/phpunit": "^8.0"
    },
    "autoload": {
        "classmap": [
            "src/",
            "data/"
        ]
    },

    "scripts": {
        "post-install-cmd": [
            "php artisan clear-compiled",
            "php artisan optimize",
            "chmod -R 777 public/"
        ]
    }
}
